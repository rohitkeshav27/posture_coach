import 'package:flutter/material.dart';

class BicepScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("BICEP Screen")),
    );
  }
}
