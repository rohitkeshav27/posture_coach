import 'package:flutter/material.dart';

class PressScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("SHOULDER PRESS Screen")),
    );
  }
}
