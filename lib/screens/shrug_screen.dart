import 'package:flutter/material.dart';

class ShrugScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("SHRUGS Screen")),
    );
  }
}
