import 'package:flutter/material.dart';

class RaiseScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("SHOULDER RAISE Screen")),
    );
  }
}
